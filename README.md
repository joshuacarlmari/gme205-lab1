# Project Title
# How to set up the virtual environment
# How to run Python scripts
# Edited on GitHub web interface
# Reflection
<<<<<<< HEAD
# I chose to inspect the input coordinates because they must satisfy specific limits. In the CSV file, the second and third columns correspond to longitude and latitude values that are approximately located around UP Diliman. The script automatically checks the data for inconsistencies, but human inspection is still necessary to verify both the dataset and the code. As the dataset becomes very large, not all points may be plotted, especially if some coordinates fall outside the required limits that must be satisfied.
=======
# I chose to inspect the input coordinates because they must satisfy specific limits. In the CSV file, the second and third columns correspond to longitude and latitude values that are approximately located around UP Diliman. It was checked by the numbers of rows and columns, and column names. The script automatically checks the data for inconsistencies in codes and data sets, but human inspection is still necessary to verify both the dataset and the code just like the data set for the maximun lattitude and longitude. As the dataset becomes very large, not all points may be plotted, especially if some coordinates fall outside the required limits that must be satisfied.
>>>>>>> 338a7d47150b77c09cb54286dbc598064a18fcff
